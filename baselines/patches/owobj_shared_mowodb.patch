diff --git a/datasets/torchvision_datasets/open_world.py b/datasets/torchvision_datasets/open_world.py
--- a/datasets/torchvision_datasets/open_world.py
+++ b/datasets/torchvision_datasets/open_world.py
@@ -145,16 +145,9 @@ class OWDetection(VisionDataset):
         annotation_dir = os.path.join(self.root, 'Annotations')
         image_dir = os.path.join(self.root, 'JPEGImages')
 
-        if "test" in image_set:
-            self.coco = COCO('/g/data/cp23/datasets/coco/annotations/instances_val2017.json')
-        else:
-            self.coco = COCO('/g/data/cp23/datasets/coco/annotations/instances_train2017.json')  
-        tag="val2017" if "test" in image_set else "train2017"
-        for floder_name in fo.keys():
-            if floder_name==tag: #"trainval", "train2017" 
-                self.hdf5=fo[floder_name]
-                break
-        splits_dir = "data/OWDETR/coco/ImageSets"
+        # Tree-DETR shared M-OWODB adapter: use the explicit local data root
+        # and VOC XML generated from the official stage annotations.
+        splits_dir = os.path.join(self.root, 'ImageSets', self.dataset)
         file_names = self.extract_fns(image_set, splits_dir)
         if image_set == 'voc2007_trainval':
             print('PASCAL-VOC2007 dataset used; clearing images with missing object classes')
@@ -214,12 +207,14 @@ class OWDetection(VisionDataset):
 
     @functools.lru_cache(maxsize=None)
     def load_instances(self, img_id):
-        # tree = ET.parse(self.imgid2annotations[img_id])
-        # target = self.parse_voc_xml(tree.getroot())
-        target=self.coco_annotation(os.path.basename(self.imgid2annotations[img_id]).split('.')[0])
+        tree = ET.parse(self.imgid2annotations[img_id])
+        target = self.parse_voc_xml(tree.getroot())
         image_id = target['annotation']['filename']
         instances = []
-        for obj in target['annotation']['object']:
+        objects = target['annotation'].get('object', [])
+        if isinstance(objects, dict):
+            objects = [objects]
+        for obj in objects:
             cls = obj["name"]
             if cls in VOC_CLASS_NAMES_COCOFIED:
                 cls = BASE_VOC_CLASS_NAMES[VOC_CLASS_NAMES_COCOFIED.index(cls)]
@@ -278,27 +273,6 @@ class OWDetection(VisionDataset):
             if annotation["category_id"] not in known_classes:
                 annotation["category_id"] = total_num_class - 1
         return entry
-    def coco_annotation(self,image_id):
-        target={"annotation":{}}
-        coco= self.coco
-        image_details =coco.imgs[int(image_id)]
-        file_name=image_details['file_name']
-        target["annotation"]["filename"]=file_name
-        keys=["width","height","depth"]
-        values=[str(image_details['width']),str(image_details['height']),str(3)]
-        target["annotation"]["size"]=dict(zip(keys,values))
-        target["annotation"]["object"]=[]
-        for annotation in coco.imgToAnns[int(image_id)]:
-            obj=dict()
-            obj['name']=coco.cats[annotation['category_id']]['name']
-            obj['difficult']= '0'
-            obj['bndbox']= {}
-            obj['bndbox']['xmin']=str(int(annotation['bbox'][0] + 1.0))
-            obj['bndbox']['ymin']= str(int(annotation['bbox'][1] + 1.0))
-            obj['bndbox']['xmax'] = str(int(annotation['bbox'][0] + annotation['bbox'][2] + 1.0))
-            obj['bndbox']['ymax'] = str(int(annotation['bbox'][1] + annotation['bbox'][3] + 1.0))
-            target["annotation"]["object"].append(obj)
-        return target
     def __getitem__(self, index):
         """
         Args:
@@ -309,11 +283,7 @@ class OWDetection(VisionDataset):
         """
 
         image_set = self.transforms[0]
-        file_name=self.image_set[index]#os.path.basename(self.images[index]).split(".")[0]
-        jpeg = self.hdf5[file_name][:]
-        img = Image.open(BytesIO(jpeg)).convert('RGB')
-        # img.save("img.png")
-        # img = Image.open(self.images[index]).convert('RGB')
+        img = Image.open(self.images[index]).convert('RGB')
         target, instances = self.load_instances(self.imgids[index])
         if 'train' in image_set:
             instances = self.remove_prev_class_and_unk_instances(instances)
diff --git a/main_open_world.py b/main_open_world.py
--- a/main_open_world.py
+++ b/main_open_world.py
@@ -401,10 +401,8 @@ def get_datasets(args):
 
     train_set = args.train_set
     test_set = args.test_set
-    file='/g/data/cp23/datasets/fcoco/coco_all_q95.hdf5'
-    fo= h5py.File(file,"r")
-    dataset_train = OWDetection(args, args.data_root, image_set=args.train_set, transforms=make_coco_transforms(args.train_set), dataset = args.dataset,fo=fo)
-    dataset_val = OWDetection(args, args.data_root, image_set=args.test_set, dataset = args.dataset, transforms=make_coco_transforms(args.test_set),fo=fo)
+    dataset_train = OWDetection(args, args.data_root, image_set=args.train_set, transforms=make_coco_transforms(args.train_set), dataset = args.dataset)
+    dataset_val = OWDetection(args, args.data_root, image_set=args.test_set, dataset = args.dataset, transforms=make_coco_transforms(args.test_set))
 
     print(args.train_set)
     print(args.test_set)
